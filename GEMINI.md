

## 工作
- 你应该独自工作，独立决策
- 不要执行 `npm create` 这样的命令，会需要用户批准（因此会阻碍住你）。你自己直接生成文件。
- `npm run dev` 显然会卡住你，那么就使用 `npm run build` 然后使用 skill screenshot 来截图，然后 read_file 这个截图文件 看看前端效果，并自己调整视觉效果
- git 提交 并且push